/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any image domains here if you use next/image with external URLs
  images: {
    domains: [],
  },
};

module.exports = nextConfig;
